object zad23 extends App {
  /*def indices[A](seq: Seq[A], el: A): Set[Int] = {
    val (res, _) = seq.foldLeft((Set[Int](), 0))((acc, e) => acc match {
      case (e, n) =>
      if(e == el)
    }
  }*/
}